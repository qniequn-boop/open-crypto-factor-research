# Research contracts use byte-level SHA-256 fingerprints. Keep text identical
# across Windows and Linux checkouts so a clone does not invalidate a freeze.
* text=auto eol=lf

*.png binary

# This frozen evidence object is fingerprinted exactly as generated.
logs/panel_literature_replication_batch005_20260716.json -text !eol
